<Window x:Class="HealthCare.View.AppointmentView.PatientRecordView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HealthCare.View.AppointmentView"
        mc:Ignorable="d"
        Title="PatientRecordView" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.15*"/>
            <RowDefinition Height="0.8*"/>
        </Grid.RowDefinitions>
        <ListView x:Name="listViewRecord" Grid.Row="2">
            <ListView.View>
                <GridView>
                    <GridViewColumn  Header="Datum pregleda" DisplayMemberBinding="{Binding TimeSlot.Start}"/>
                    <GridViewColumn  Header="Ime doktora" DisplayMemberBinding="{Binding Doctor.Name}"/>
                    <GridViewColumn  Header="Specijalizacija" DisplayMemberBinding="{Binding Doctor.Specialization}"/>
                </GridView>
            </ListView.View>
        </ListView>

    </Grid>
</Window>
